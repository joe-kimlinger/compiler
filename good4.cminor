main: function integer () = {
	t: boolean = true;
	f: boolean = false;
	if (t && t)
		print "1Should print\n";
	if (t && f)
		print "2Should not print\n";
	if (f && f)
		print "3Should not print\n";
	if (f || f)
		print "4Should not print\n";
	if (!f || f)
		print "5Should print\n";
	if (t || f)
		print "6Should print\n";
	if (!t || f)
		print "7Should not print\n";
	if (t && f || t && !false)
		print "8Should print\n";
}
