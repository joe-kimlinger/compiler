main: function integer() = {
	s: string = "hello";
	b: string = "there";
	x: string = s;
	if (s == x)
		print "1: should print\n";
	if (s == b)
		print "2: should not print\n";
	if (s == "hello")
		print "3: should print\n";
	if (b == "hello")
		print "4: should not print\n";
}
